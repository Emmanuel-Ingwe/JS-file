for (let p = 1; p <= 10; p++) {
    console.log('gods Loop:', p);
    for (let q = 10; q >= 0; q -= 2) {
        console.log('  godess Loop', q);
    }
}

const sub = ['team', 'plays', 'diamond'];
const subb = ['-player', ' basketball', '-castle'];
for (let i = 0; i < sub.length; i++) {
    console.log(`${sub[i]}${subb[i]}`);
}

const x = 6;

if (x === 5) {
    console.log('monkey');
} else if (x < 5) {
    console.log('small monkey');
} else {
    console.log('lost creature');
}

const sumNum = (num1 = 1, num2 = 1) => {
    console.log(num1 + num2);
};

sumNum(5.633564, 2333333335);

function Man(firstName, lastName, Ntnly) {
    this.firstName = firstName;
    this.lastName = lastName:
}

const Man1 = first Man(Charlie, bobo, kenyan);
